const config = {
    LicenseManagementApp: {
      /* don't expose password or any sensitive info, done only for demo */
      host: 'localhost',
      user: "root",
      password: "Arfasara1624928",
      database: "LicenseManagementApp",
    },
    listPerPage: 10,
  };
  module.exports = config;